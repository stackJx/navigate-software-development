# 变更日志

本文件记录开发罗盘的方法骨架与门禁行为变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

版本语义按**门禁行为**而非文档字数：

- **主版本**：三个必选章节或阶段划分本身改变；
- **次版本**：门禁判定条件、新增模块或输出形态调整；
- **修订版本**：措辞澄清、示例补充、不改变任何判定结论。

## [0.5.0] - 2026-08-04

### 新增

- 新增 `references/8-knowledge-base.md`：**知识沉淀模块**，每次产生代码变更的开发在阶段 B 收尾维护目标仓库的知识库文档（默认 `KNOWLEDGE.md`），把关键决策与理由、非显而易见的坑与约束、领域规则、外部契约等"离开代码就会丢失、未来会被再次需要"的信息凝聚入库。
- 知识沉淀采用**默认开启 + 值得沉淀过滤**：每次开发都评估，但只有过纳入清单才写；纯分析、只读调查、无行为变更的微改或确无可沉淀知识时跳过并写一句理由。知识库是被持续凝聚的当前事实（同主题更新/合并、过时修正或删除），不是 append-only 流水账，与 CHANGELOG、CLAUDE.md、个人 memory 职责区分。
- 主路由新增「知识沉淀（默认开启的收尾维护）」一节；阶段 B 流程图加"知识沉淀"、`交付格式/最终答复`新增第 3 项「本次沉淀」、`跨阶段追踪`链尾延伸 `→ 知识沉淀`、`分发与执行规则`新增第 7 条。
- `tests/gate-cases.md` 新增 G 组「知识沉淀」回归用例（该写 / 纯分析跳过 / 微改跳过 / 同主题更新而非新增 / 排除可被代码自证的信息），F 组新增一条易错点。
- `README.md` 模块表、固定骨架、"它解决什么问题"、设计原则、输出约定、目录结构同步更新。

### 变更

- frontmatter `description` 摘要补充"完成后把有价值信息凝聚进仓库知识库"。

### 说明

- 版本语义为**次版本**：新增一个默认开启的收尾模块并调整最终答复输出形态（新增「本次沉淀」项），未改动三个必选章节或两阶段骨架本身。

## [0.4.0] - 2026-08-04

### 变更

- **Git Flow 门禁改为"仅显式声明才命中"**：此前"多人协作 / 存在共享主干 / 仓库已有分支模型 / 用户明确要求"任一即命中；现收紧为**仅当用户在本次请求中显式声明要用版本控制流程**（点名 Git Flow / 分支模型 / 提交规范 / PR / 评审 / 合并与主干保护，或要求把变更合入受保护分支）才命中。仓库客观事实本身不再触发，未显式声明一律跳过并写一句理由，把版本控制流程的启用权交还用户。DDD、TDD 门禁不受影响。
- `references/7-git-flow.md` §1 适用性门禁同步：入口明确"仅显式声明才进入本模块"，L0–L2 投入分档保留为命中后的深度选择。
- `tests/gate-cases.md` A 组回归用例随新判定更新：A2/A3/A6/A7 由"按协作形态判定/命中"改为跳过（未显式声明）；A8 由"跳过 L0（单人）"改为跳过（未显式声明，裸提交非策略声明）；A9 保留命中 L2，理由改为"用户显式要求合入受保护主干"。F 组新增"Git Flow 仅显式声明才命中"易错点，并修正"档位不由规模决定"中已失效的 A8 示例。
- `README.md`（第 5 行、模块表、固定骨架两条）与 `references/5-worked-example.md`（对照表示例二/三、通用形态）中 Git Flow 的判定描述统一改为"仅在用户显式声明时启用"。

### 说明

- 版本语义为**次版本**：改变了 Git Flow 单个门禁的判定条件，未改动三个必选章节、两阶段骨架或 DDD/TDD 门禁。
- frontmatter `description` 触发词未改——其中 Git Flow 相关词本就只在用户点名时激活 Skill，与新判定天然一致。

## [0.3.0] - 2026-08-01

### 新增

- 新增 `references/7-git-flow.md`，作为与 DDD、TDD 并列的第三个可选门禁模块。覆盖分支模型选择（直接主干 / GitHub Flow / git-flow 三模型并列，技术栈中立）、提交规范（Conventional Commits 中立化，可适配项目既有约定）、合并与主干保护、与 TDD/委派/微变更的衔接。
- Git Flow 采用 **L0–L2 三档投入分档**：L0 直接提交（单人/实验性）、L1 短分支（多人协作纵向切片）、L2 受保护主干（PR/评审/CI）。档位由协作形态与共享主干风险决定，不由仓库规模决定，允许结论是"直接主干提交"。
- 主路由新增「Git Flow 选择门禁」一节；阶段 A 流程图、分发与执行规则、开发前合同自检表第 7 条、跨阶段追踪、交付格式均更新为"DDD/TDD/Git Flow 三门禁"。
- 路由表新增「分支策略、提交规范、PR/评审、合并与主干保护」一行。
- `references/3-development-planning.md` 按需追加项新增「版本控制策略」，未命中时写一句跳过理由。
- `tests/gate-cases.md` A 组新增 A8（单人仓库全跳过）、A9（受保护主干 L2）；C 组新增 C6（建分支属变更动作）；F 组新增三条易错点（三门禁独立、Git Flow 档位不由规模决定、建分支是变更动作）。
- `README.md` 模块表、固定骨架、目录结构、设计原则同步更新。

### 变更

- frontmatter `description` 与各处"DDD 与 TDD"表述统一改为"DDD、TDD 与 Git Flow"。
- 模块专有门禁指针列表新增版本控制模块入口。

## [0.2.0] - 2026-07-27

### 新增

- 新增 `references/6-delegation.md`，子智能体委派从主文件抽出并按平台分写（Codex `spawn_agent` / Claude Code `Agent` / 未知平台降级 solo）。主文件只保留门禁摘要与指针。
- 新增 `tests/gate-cases.md`，覆盖门禁组合、微变更快速通道、阶段边界、委派降级与不应触发五组回归用例，供改动后自查判定漂移。
- 新增**微变更快速通道**：单文件 + 无行为变更 + 可一眼核验 + 不触及权限/数据正确性/迁移/外部契约/依赖版本，四条全中时三章节可压缩为各一到两行，仍须在变更前输出。
- 新增**开发前合同自检表**（8 条跨模块通用项）收口原先分散在四个 reference 中的 35+ 条门禁；各 reference 只保留本模块专有项。
- 新增**位置判据**：三个二级标题必须是该轮回复的首个文本块，其后才允许出现写工具调用。
- `references/2-ddd-design.md` 新增场景速查表（对齐 TDD 模块），命中 DDD 门禁不再需要通读全文。
- `references/5-worked-example.md` 新增示例三（满减券边界缺陷：TDD 命中 + DDD 跳过），补齐日常最高频的门禁组合。
- `CHANGELOG.md`（本文件）。

### 变更

- **修复阶段冲突**：明确「运行测试属于阶段 B 的第一步」。此前 TDD 模块要求先建绿灯基线，与「合同前禁止写操作」冲突（测试运行会写缓存/测试库/覆盖率产物）。阶段 A 判断 TDD 门禁时只读测试文件与既有 CI 结果。
- DDD 投入门禁表重构为 **L0–L4 五档**，每档标注最小交付物编号；`§11` 交付物清单改为带编号列表以供引用。档位由问题性质决定，不由项目规模决定。
- 「开发思路」输出拆分为**必给四项**（现状与缺口 / 实现范围与落位 / 纵向切片与顺序 / 验证方式与风险）+ **按需七项**（含触发条件表），避免中小任务被撑成模板文。
- `references/4-tdd.md` 开头新增示例栈声明，显式标注命名与替身 API 示例取自 PHP/Laravel + PHPUnit，仅作形态参考；场景速查表与浏览器等待条目去除框架专属 API 名。
- 路由表新增微变更一行。
- 压缩 frontmatter `description`（约 250 → 185 字），移除近义触发词，降低常驻 context 开销。

### 修复

- `references/5-worked-example.md` 的门禁组合说明此前只覆盖两个极端（全跳过 / 全命中），缺少最高频的中间态。

## [0.1.0] - 2026-07 之前

初始版本。建立带前置门禁的两阶段骨架：只读调查 → 需求分析 → DDD/TDD 门禁 → 开发思路 → 验收标准 → 实施与验证。包含需求分析、DDD 设计、开发规划、TDD 四个模块与一份走查示例。
