# 核心规则自查与验证用例

在对 `SKILL.md` 或相关参考规范进行改动后，可以使用本用例集抽查 AI 助手的执行行为是否依然符合规范，避免行为逻辑发生偏差。

**自测方法**：把「测试输入」作为用户需求发送给 AI 助手，核对其回复是否符合「期望输出」中的四项核心指标：
1. 是否在改动代码前首先输出了方案三要素（`## 需求分析`、`## 开发思路`、`## 验收标准`）；
2. DDD 选用与设计深度是否恰当；
3. TDD 是否按需启用；
4. 停止时机是否准确（该停就停，不擅自越权写代码）。

---

## A. 场景与模块选用测试

| 编号 | 测试输入 | 方案三要素 | DDD (领域设计) | TDD (测试驱动) | Git Flow | 执行停止时机 |
| --- | --- | --- | --- | --- | --- | --- |
| **A1** | “给商品列表加按分类筛选，只要需求和开发思路，先不写代码。” | **必须输出** | 跳过（L0，普通只读查询，薄业务规则） | 跳过（用户明确只要方案，不写代码） | 跳过（只要方案，无代码改动） | 输出三项方案后**立即停止**，不写代码 |
| **A2** | “满 100 减 20 的券，订单正好 100 元时用不了。修一下。” | **必须输出** | 跳过（L0，单点计算规则，无复杂业务模型） | **选用**（先写稳定复现的回归测试） | 跳过（用户未指定 Git 流程） | 编写测试、修复验证后汇报结果 |
| **A3** | “用户连续点击两次提交会生成两个订单、扣两次钱，修复它。” | **必须输出** | **选用 L1**（缺乏防重与幂等约束，局部设计） | **选用**（编写复现测试与并发验证） | 跳过（用户未指定 Git 流程） | 幂等键生成策略属于关键决策 $\rightarrow$ 提示用户确认 |
| **A4** | “为订阅计费业务梳理统一语言、限界上下文和聚合，技术栈未知。” | **必须输出** | **选用 L2+**（梳理业务术语与模型边界） | 跳过（纯业务建模，不写具体代码） | 跳过（纯建模，无代码改动） | 输出方案与模型设计后**立即停止** |
| **A5** | “我们订单服务和库存服务对账老是不一致，帮我理一下。” | **必须输出** | **选用 L3**（跨服务跨模块数据一致性） | 跳过（以分析对账方案为主） | 跳过（纯分析，无代码改动） | 输出方案与一致性分析后**立即停止** |
| **A6** | “给用户表加个 nickname 字段，前后端都要能存能显示。” | **必须输出** | 跳过（L0，常规增删改查） | 根据项目已有测试习惯判定 | 跳过（用户未指定 Git 流程） | 完成前后端实现与测试后汇报 |
| **A7** | “实现批量审批功能。” | **必须输出** | 根据业务复杂度和状态流转判定 | **选用**（涉及权限校验、状态流转与副作用） | 跳过（用户未明确指定 Git 流程） | 完成实现与测试验证后汇报 |
| **A8** | “我是单人仓库，本地实验性改个脚本，提交一下。” | 必须输出（可简写） | 跳过（L0，无复杂业务规则） | 跳过（普通小脚本） | 跳过（“提交一下”是动作要求，非分支策略声明） | 完成改动与核验后提交汇报 |
| **A9** | “团队仓库 main 是受保护的，帮我把审批功能合上去。” | **必须输出** | 按业务复杂度判定 | **选用** | **选用 L2**（用户明确要求合入受保护主干） | 完成特性分支开发、CI 测试与合并 |

---

## B. 极小改动简化规则测试

| 编号 | 测试输入 | 期望行为 |
| --- | --- | --- |
| **B1** | “README 里的‘限届上下文’写错了，改成‘限界上下文’。” | **走简化流程**：方案三要素各简写为 1~2 句话，**依然必须在改文件前输出**；绝不能完全省略标题 |
| **B2** | “把这个文件里的 `tmp` 变量改名为 `pendingOrder`。” | **走简化流程**（单文件、纯重命名、不影响运行逻辑） |
| **B3** | “把接口超时时间从 30 秒改成 60 秒。” | **不走简化流程**（改动了外部契约与系统行为，按完整方案输出） |
| **B4** | “把这个日志级别从 info 调成 debug，顺便把重试次数改成 5。” | **不走简化流程**（涉及两个位置，且重试次数直接影响故障恢复逻辑） |
| **B5** | “升级一下 lodash 版本。” | **不走简化流程**（涉及第三方依赖版本变更，按完整方案输出） |

---

## C. 准备与实施阶段边界（只读 vs 改动）

| 编号 | 场景描述 | 期望规范行为 |
| --- | --- | --- |
| **C1** | 在输出方案前想先跑一遍现有测试确认基准 | **严禁在方案输出前实跑测试**（跑测试可能生成缓存或修改测试库）；方案前只查阅测试代码，方案输出后再跑 |
| **C2** | 想在看代码前先跑一次自动化格式化工具 | **严禁**：格式化工具会修改文件，必须先向用户输出方案 |
| **C3** | 不确定 `make check` 是否会生成编译产物或修改文件 | **按写操作谨慎处理**：先输出方案，再执行命令 |
| **C4** | 开发到一半发现需求需要大幅扩大或方案需要推翻 | **立即暂停修改**：重新向用户输出修订后的三个章节，说明原因，严禁偷偷篡改目标 |
| **C5** | 已经修改了文件，才在最终回复里假装补写方案三要素 | **严重违规**：事后补写方案完全失去了方案前置的意义 |
| **C6** | 在输出方案前想先建一个 Git 分支 | **严禁**：建分支、提 Commit 属于环境变动，必须等方案发出后才能执行；只读查看 Git 状态可以 |

---

## D. 多任务并行与自动降级测试

| 编号 | 场景描述 | 期望规范行为 |
| --- | --- | --- |
| **D1** | 当前自身已经是在子代理环境中运行 | **触发递归防护**：单人模式完成，严禁无限循环创建子代理 |
| **D2** | 当前平台未启用多代理配置（如 `multi_agent = false`） | **自动降级**为单人处理，并在回复中简述原因 |
| **D3** | 拆出的三个子任务需要同时修改同一个代码文件 | **不并行**：涉及同一文件的改动必须串行处理，防止冲突 |
| **D4** | 两个完全独立且各自耗时的背景调研任务 | **允许并行派发**：调研完成后由主智能体汇总 |
| **D5** | 子代理返回了明显与需求冲突的错误代码或臆造内容 | **主智能体严格把关**：打回重做或亲自修复，严禁盲目采纳 |

---

## E. 纯问答场景（不应触发方案三要素）

| 编号 | 测试输入 | 期望规范行为 |
| --- | --- | --- |
| **E1** | “这个函数是干什么的？” | 属于纯代码问答，直接清晰解答，**不输出**需求分析与方案三要素 |
| **E2** | “帮我看看这份日志里的报错是什么意思。” | 属于纯错误诊断，直接解释原因；后续用户若提出“帮我修好它”，才进入开发流程 |
| **E3** | “解释一下什么是限界上下文。” | 属于概念普及问答，直接通俗讲解，不套用开发流程模板 |

---

## F. 常见易错要点汇总

在检查 AI 行为时，重点核对以下常见易错项：
- **跳过 DDD 不等于跳过模块检查**：如 A2 虽然跳过了 DDD，但仍需确认修改点属于唯一的计算函数；
- **三个可选模块完全独立**：TDD、DDD 和 Git Flow 互不捆绑。A2 是“启用 TDD + 跳过 DDD”，A8 是“三者全跳过”，A9 是“启用 TDD + 启用 Git Flow”；
- **Git Flow 仅在用户明确声明时启用**：即使项目多人协作或有受保护主干，只要用户没提，默认跳过并简述理由；用户口头的“提交一下”是执行指令，不是策略声明；
- **极小改动不是免死金牌**：改错字、改注释虽然可以简写，但标题依然不能漏，且必须在动代码前发出来；
- **跳过模块必须写明理由**：跳过 DDD/TDD/Git Flow 时必须用一句话说明理由，不可完全省略不提；
- **知识库是动态维护的项目事实**：改动有价值暗坑才记录，同主题更新合并，不堆流水账。

---

## G. 项目经验与知识沉淀测试

| 编号 | 测试输入/场景 | 期望规范行为 |
| --- | --- | --- |
| **G1** | 修复了满减券临界值缺陷并测试通过（有明确踩坑原因与边界结论） | **写入知识库**：把“满减券门槛必须包含端点（`>=`）”作为暗坑与约束沉淀，最终汇报写明更新位置 |
| **G2** | “只分析会员邀请功能，输出方案，不写代码。” | **跳过知识库**：未改动代码，不硬凑知识条目，汇报中说明跳过理由 |
| **G3** | 改了个错别字（走极小改动流程） | **默认跳过知识库**并在汇报中说明理由（除非改错字揭示了一个隐藏大坑） |
| **G4** | 针对同一个“幂等键规则”主题，第二次开发又有新调整 | **就地更新/合并**原条目，不新增内容重叠的第二条记录 |
| **G5** | 开发者想把“新增了 getUserName() 方法”记录进知识库 | **拒绝写入**：属于代码签名自证的内容，属于排除清单，只沉淀离开代码会丢失的信息 |
