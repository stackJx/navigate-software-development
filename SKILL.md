---
name: navigate-software-development
description: 软件开发方法路由器：开发、修复或重构前，先向用户可见输出「需求分析」「开发思路」「验收标准」三项开发前合同，再按合同实施与验证，完成后把有价值信息凝聚进仓库知识库；DDD、TDD 与 Git Flow 按门禁可选加入。触发——需求分析、验收标准、用例梳理、开发方案、模块拆分；DDD/领域驱动设计、限界上下文、四色建模、聚合、领域事件、防腐层、六边形架构、Saga；TDD、测试先行、测试分层、Mock/Fake、回归测试；分支策略、提交规范、PR/评审、合并与主干保护、Git Flow；功能开发、Bug 修复、重构。禁止先开发再补写分析。
---

# 开发罗盘

带前置门禁的阶段路由器。每次先建立并向用户输出**需求分析、开发思路、验收标准**，然后才允许开发；**DDD**、**TDD** 与 **Git Flow** 由任务门禁决定是否加入。

## 固定执行合同

每次触发严格分成两个阶段。可以先做只读调查以获得代码证据，但不能先产生变更：

```text
阶段 A：只读调查 → 需求分析 → DDD/TDD/Git Flow 门禁 → 开发思路 → 验收标准
                         ↓
             先发送一条用户可见的开发前合同
                         ↓
阶段 B：绿灯基线 → 实现 → 验证 → 知识沉淀 → 对照验收标准汇报
```

1. 先检查用户请求、仓库约定、代码、测试和文档。此时只允许搜索、读取、状态检查和其他不改变工作区或外部状态的操作。
2. 读取 [1-requirements-analysis.md](references/1-requirements-analysis.md) 建立需求与可判定验收条件。
3. 检查 DDD、TDD 与 Git Flow 门禁；命中 DDD 时读取 [2-ddd-design.md](references/2-ddd-design.md)，命中 TDD 时读取 [4-tdd.md](references/4-tdd.md)，命中 Git Flow 时读取 [7-git-flow.md](references/7-git-flow.md)；未命中时分别准备一句跳过理由。
4. 读取 [3-development-planning.md](references/3-development-planning.md)，吸收选中的领域决策、测试先行顺序与版本控制策略，形成可执行开发思路。
5. 从需求用例中提炼独立、二元可判定且与开发步骤可追踪的验收标准。
6. **在首个变更动作之前**，发送一条独立、用户可见的开发前消息，依次使用以下二级标题：
   - `## 需求分析`
   - `## 开发思路`
   - `## 验收标准`
7. 用户要求实现时，开发前消息发出后立即按合同实施，无需等待确认；存在阻塞性未知项、不可逆/高风险动作或用户明确要求审批时，先等待用户决定。
8. 只要求分析或方案时，交付三项开发前合同后停止，不进行任何变更。

产物可按复杂度缩放；不确定输出形态时，对照 [5-worked-example.md](references/5-worked-example.md)。

### 位置判据（可机械核验）

三个二级标题必须是**该轮回复的首个文本块**，其后才允许出现任何写工具调用。出现以下任一情况即为违规：

- 本轮已执行写工具调用，三个标题才出现；
- 三个标题被改成加粗、被合并成一个章节、或只写进路由说明与内部推理；
- 三项在最终答复中才首次出现——**发送最终答复不算满足前置门禁**。

### 变更动作边界

- 允许在开发前合同之前执行：浏览目录、读取文件、搜索、查看 Git 状态/差异/历史、读取测试文件与既有 CI 结果。
- 禁止在开发前合同之前执行：创建或编辑文件、自动修复/格式化、代码生成、安装或升级依赖、启动会写缓存/构建产物的任务、提交/推送、数据库迁移、发布，以及任何外部写操作。
- **运行测试属于阶段 B 的第一步，不在阶段 A 执行。** 测试运行通常会写缓存、建测试库或生成覆盖率产物。阶段 A 判断 TDD 门禁时只读测试文件与既有 CI 结果，不实跑；[4-tdd.md](references/4-tdd.md) 的绿灯基线在开发前合同发出之后建立。
- 无法确认某命令是否会写入时，按变更动作处理，先输出开发前合同。
- 实施中发现需求、范围、关键方案或验收标准需要改变时，暂停变更，先向用户重新输出修订后的三个章节，再继续实施；不能在最终答复里静默改写基线。

### 微变更快速通道

**同时**满足以下全部条件时，三项合同可压缩为各一到两行，但仍须在变更前输出：

- 只涉及单个文件；
- 不改变任何可观察行为（错别字、注释、格式、纯重命名、展示文案、无逻辑分支的配置项）；
- 正确性可由人一眼核验，不需要运行测试来证明；
- 不触及权限、数据正确性、并发、迁移、外部契约或依赖版本。

任一条不满足即走完整合同。快速通道只缩短篇幅，**不跳过门禁**——三个标题仍必须出现，且仍在首个变更动作之前。判断不确定时走完整合同。

## 路由表

| 用户意图 | 开发前合同 | 默认停止点 |
| --- | --- | --- |
| 需求分析、范围、角色、用例、验收标准 | 三项必选；开发思路可简短 | 输出三项合同，不擅自编码 |
| 开发思路、开发方案、模块梳理、优先级、MVP | 三项必选 | 输出三项合同，不擅自编码 |
| DDD、领域建模、上下文映射、聚合、领域事件、六边形架构、领域模式 | 三项必选；开发思路纳入领域设计 | 输出合同及领域设计，不擅自编码 |
| TDD、测试先行、测试命名/设计、测试替身、回归测试 | 三项必选；开发思路纳入测试先行顺序 | 输出合同及测试设计；实现按用户要求 |
| 分支策略、提交规范、PR/评审、合并与主干保护 | 三项必选；开发思路纳入版本控制策略 | 输出合同及版本控制策略；实现按用户要求 |
| 完整功能开发或重构 | 先输出三项，再开发 | 完成实现与验证 |
| Bug 修复 | 先输出三项，再修复 | 完成修复与验证 |
| 无行为变更的微变更（命中快速通道全部条件） | 三项压缩为各一到两行，仍在变更前输出 | 完成变更与一眼核验 |

## DDD 选择门禁

满足任一条件时加入 DDD 模块：

- 用户明确要求 DDD、四色建模、聚合、分包或领域模式；
- 存在丰富业务规则、状态迁移、不变量或长期演进的统一语言；
- 存在多业务能力、模型所有权冲突、跨模块一致性或外部系统防腐需求；
- 问题本质是上下文边界、聚合边界、依赖方向、职责放置或跨边界一致性错误。

简单 CRUD、一次性原型、纯技术适配或规则很薄时跳过 DDD 并简述理由。跳过 DDD 不等于跳过模块边界与依赖检查。命中时先按 [2-ddd-design.md](references/2-ddd-design.md) 的**投入分档表**确定档位与最小交付物，只做与问题相关的那一档，不必每次铺满全套战略建模（见 [5-worked-example.md](references/5-worked-example.md) 示例二）。

## TDD 选择门禁

满足任一条件时加入 TDD 模块：

- 用户明确要求 TDD、测试先行或回归测试；
- 用户要求设计或改进测试命名、结构、分层、夹具、替身或可读性；
- 正在实现或修复业务规则、权限、状态、边界值、幂等、并发或副作用；
- 代码库已有可运行测试，变更需要回归保护；
- Bug 可先用稳定失败用例复现。

纯需求分析、纯开发计划、探索性原型或非可执行文档不进入红—绿—重构。若用户明确不要测试，遵从并报告未覆盖风险。

## Git Flow 选择门禁

**仅当用户在本次请求中显式声明要用版本控制流程时才加入 Git Flow 模块。** 满足任一即视为显式声明：

- 用户点名 Git Flow、某种分支模型（trunk-based / GitHub Flow / git-flow 等）或要求分支隔离；
- 用户要求提交规范、提交粒度或 Conventional Commits；
- 用户要求 PR、评审、合并方式或主干保护；
- 用户要求把变更合入某个受保护／共享分支并遵循其规则。

**仓库客观事实本身不再触发 Git Flow。** 即便多人协作、存在共享主干（`main`/`master`/`develop` 等）、或仓库已有分支模型与保护规则，只要用户未显式声明，一律跳过 Git Flow 并写一句理由（可附一句"如需分支／提交／合并策略请显式说明"）。这样把版本控制流程的启用权交还用户，不给未提出的请求强加流程。

显式声明命中时，先按 [7-git-flow.md](references/7-git-flow.md) 的**投入分档表**确定档位与最小交付物，只做与本次请求相关的那一档，不必每次铺满完整 git-flow。Git Flow 与 DDD、TDD、委派独立——各自按门禁命中与否，允许任意组合。

## 知识沉淀（默认开启的收尾维护）

每次产生代码变更的开发，在阶段 B 验证之后评估"本次有没有值得沉淀的知识"，把关键决策与理由、非显而易见的坑与约束、领域规则、外部契约等**离开代码就会丢失、未来会被再次需要**的信息，凝聚进目标仓库的知识库文档（默认 `KNOWLEDGE.md`，复用仓库既有约定）。

**默认开启不等于每次都写**：只有过了纳入清单才落笔；纯分析、只读调查、无行为变更的微改，或本次确无可沉淀知识时，跳过并在最终答复写一句理由。知识库是被持续凝聚的当前事实（同主题更新/合并、过时修正或删除），不是 append-only 流水账，也不替代 CHANGELOG 与 CLAUDE.md。纳入/排除清单、文件与条目结构、去重与凝聚规则见 [8-knowledge-base.md](references/8-knowledge-base.md)。

## 子智能体委派（可选执行优化）

复杂任务可把独立、无共享状态的子任务委派给子智能体并行推进，再扇入合并回三个必选章节。这是**执行层的可选优化，不是额外的方法阶段**：门禁逻辑与三个开发前必选章节一律不变，**默认 solo**。

委派门禁、平台能力映射（Codex `spawn_agent` / Claude Code `Agent`）、检测降级、分阶段拆分建议与扇入验收规则见 [6-delegation.md](references/6-delegation.md)。只在确实考虑并行时读取该文件。

## 分发与执行规则

1. 读取用户请求、现有代码、文档与测试，区分事实、推断与未知项；先检查现有仓库约定，再提出结构或改代码。
2. 始终建立并在变更前输出需求分析、开发思路、验收标准；只对 DDD、TDD 与 Git Flow 应用门禁。上游产物可信时复用，不重复调查，但要摘要本次采用的结论。
3. 业务事实必须有用户输入、现有代码、测试或文档证据，不臆造规则；非阻塞未知项可采用明确、可逆的安全假设，阻塞性业务决策再询问用户。
4. 优先交付最小纵向闭环；权限、数据正确性与必要失败恢复不能以 MVP 为由删除。
5. 设计模式由变化点或复杂度证据驱动，允许结论是"不使用模式"；适配目标项目的语言、框架与目录习惯，不机械复制任何示例结构。
6. 保留并尊重现有测试，不能为绿灯弱化断言、吞异常或跳过测试。
7. 每次产生代码变更的开发，在验证后评估是否有值得沉淀的知识，并按 [8-knowledge-base.md](references/8-knowledge-base.md) 维护仓库知识库（默认开启，无价值则跳过写理由）；知识库是被持续凝聚的当前事实，不是流水账。

## 开发前合同自检表

发出开发前消息之前逐条核对。此处只保留跨模块通用项，各模块专有门禁见对应 reference：

1. 核心目标与范围可观察、可验证，"本次做 / 可后置 / 明确不做"已区分；
2. 事实、推断、假设与未知项已分开，业务事实有代码、测试、文档或用户输入证据；
3. 阻塞性业务决策已显式提给用户，未擅自拍板；
4. 每个范围内用例至少进入一个开发切片，无游离设计、无遗漏用例；
5. 每个切片有修改范围、验证方式和二元可判定的完成定义；
6. 验收标准逐条二元可判定，含必要的禁止性结果，无"功能正常""性能良好"类描述；
7. DDD / TDD / Git Flow 未命中时已各写明一句跳过理由，未伪造产物；
8. 三个标题是本轮回复的首个文本块，其后才出现写操作。

模块专有门禁：[需求](references/1-requirements-analysis.md#门禁) · [领域](references/2-ddd-design.md#121-交付门禁) · [计划](references/3-development-planning.md#门禁) · [测试](references/4-tdd.md#10-扩大验证证据与门禁) · [版本控制](references/7-git-flow.md#5-门禁产物与反例)

## 跨阶段追踪

为必选阶段与实际选中的可选阶段维护最小追踪关系：

```text
需求/用例 → 模块或上下文（若有） → 开发切片 → 测试（若有） → 代码 → 验证证据 → 知识沉淀（若有可沉淀知识）
```

不得留下无对应需求的设计，也不得把范围内用例遗漏在计划之外。需求分析、开发思路和验收标准不得标注"未选"；未选 DDD、TDD 或 Git Flow 时在开发思路中标注"未选及理由"，不伪造产物。

## 交付格式

### 开发前消息

严格按以下顺序交付，三项缺一不可：

1. `## 需求分析`：核心目标、范围、关键用例、约束与未知项。
2. `## 开发思路`：现状/缺口、实现范围、纵向切片或步骤、依赖、验证方式与风险；命中 DDD/TDD/Git Flow 时在此说明采用的设计、测试先行策略与版本控制策略，未命中时简述理由。
3. `## 验收标准`：逐条列出二元可判定的完成条件，优先使用 Given/When/Then，并包含必要的禁止性结果。

### 最终答复

开发完成后只汇报：

1. **实施结果**：实际修改文件与关键决策。
2. **验收结果**：逐条对照开发前验收标准给出通过/未通过及证据。
3. **本次沉淀**：写入/更新/删除了哪些仓库知识库条目及文件位置；无可沉淀知识时写明理由（详见 [8-knowledge-base.md](references/8-knowledge-base.md)）。
4. **偏差与未决风险**：只列真实偏差、未覆盖项和需用户决定的事项。

不要在最终答复中首次输出或事后重写需求分析、开发思路、验收标准。只有用户明确要求汇总完整过程时才可简要引用，但开发前消息仍是唯一实施基线。

## 资料基础

本 Skill 综合提炼需求分析、领域设计、开发规划与测试驱动开发资料。四色建模作为连接需求分析与领域设计的标准建模步骤。[5-worked-example.md](references/5-worked-example.md) 提供技术栈中立的端到端走查，覆盖跳过路径、部分命中路径与全命中路径三种形态，用于校准输出形态。
