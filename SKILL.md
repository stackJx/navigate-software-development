---
name: navigate-software-development
description: 软件开发方法路由器：任何开发、修复或重构开始前，先向用户可见输出「需求分析」「开发思路」「验收标准」三项开发前合同，再按合同实施与验证；按门禁可选加入 DDD 领域设计与 TDD 测试驱动。触发场景——需求分析、验收标准、用例梳理、开发方案、模块拆分、分包设计；DDD/领域驱动设计、统一语言、子域、限界上下文、上下文映射、四色建模、聚合、领域事件、Repository/Port、防腐层、六边形架构、Saga/流程管理、设计模式选择；TDD、测试先行、测试命名与分层、Mock/Fake、回归测试；以及功能开发、Bug 修复、重构。禁止先开发再在最终答复中补写分析。
---

# 开发罗盘

带前置门禁的阶段路由器。每次先建立并向用户输出**需求分析、开发思路、验收标准**，然后才允许开发；**DDD** 和 **TDD** 由任务门禁决定是否加入。

## 固定执行合同

每次触发严格分成两个阶段。可以先做只读调查以获得代码证据，但不能先产生变更：

```text
阶段 A：只读调查 → 需求分析 → DDD/TDD 门禁 → 开发思路 → 验收标准
                         ↓
             先发送一条用户可见的开发前合同
                         ↓
阶段 B：实现 → 验证 → 对照验收标准汇报
```

1. 先检查用户请求、仓库约定、代码、测试和文档。此时只允许搜索、读取、状态检查和其他不改变工作区或外部状态的操作。
2. 读取 [1-requirements-analysis.md](references/1-requirements-analysis.md) 建立需求与可判定验收条件。
3. 检查 DDD 与 TDD 门禁；命中 DDD 时读取 [2-ddd-design.md](references/2-ddd-design.md)，命中 TDD 时读取 [4-tdd.md](references/4-tdd.md)；未命中时分别准备一句跳过理由。
4. 读取 [3-development-planning.md](references/3-development-planning.md)，吸收选中的领域决策与测试先行顺序，形成可执行开发思路。
5. 从需求用例中提炼独立、二元可判定且与开发步骤可追踪的验收标准。
6. **在首个变更动作之前**，发送一条独立、用户可见的开发前消息，依次使用以下二级标题：
   - `## 需求分析`
   - `## 开发思路`
   - `## 验收标准`
7. 用户要求实现时，开发前消息发出后立即按合同实施，无需等待确认；存在阻塞性未知项、不可逆/高风险动作或用户明确要求审批时，先等待用户决定。
8. 只要求分析或方案时，交付三项开发前合同后停止，不进行任何变更。

三个标题不得改成加粗、合并成一个章节、路由说明或内部推理。**发送最终答复不算满足前置门禁**；不得先编辑文件、执行会写入文件的格式化/生成命令、安装依赖、提交、推送、迁移数据或调用会改变外部状态的工具，再补写三项。产物可按复杂度缩放；不确定输出形态时，对照 [5-worked-example.md](references/5-worked-example.md)。

### 变更动作边界

- 允许在开发前合同之前执行：浏览目录、读取文件、搜索、查看 Git 状态/差异/历史、运行明确只读的检查。
- 禁止在开发前合同之前执行：创建或编辑文件、自动修复/格式化、代码生成、安装或升级依赖、启动会写缓存/构建产物的任务、提交/推送、数据库迁移、发布，以及任何外部写操作。
- 无法确认某命令是否会写入时，按变更动作处理，先输出开发前合同。
- 实施中发现需求、范围、关键方案或验收标准需要改变时，暂停变更，先向用户重新输出修订后的三个章节，再继续实施；不能在最终答复里静默改写基线。

## 路由表

| 用户意图 | 开发前合同 | 默认停止点 |
| --- | --- | --- |
| 需求分析、范围、角色、用例、验收标准 | 三项必选；开发思路可简短 | 输出三项合同，不擅自编码 |
| 开发思路、开发方案、模块梳理、优先级、MVP | 三项必选 | 输出三项合同，不擅自编码 |
| DDD、领域建模、上下文映射、聚合、领域事件、六边形架构、领域模式 | 三项必选；开发思路纳入领域设计 | 输出合同及领域设计，不擅自编码 |
| TDD、测试先行、测试命名/设计、测试替身、回归测试 | 三项必选；开发思路纳入测试先行顺序 | 输出合同及测试设计；实现按用户要求 |
| 完整功能开发或重构 | 先输出三项，再开发 | 完成实现与验证 |
| Bug 修复 | 先输出三项，再修复 | 完成修复与验证 |

## DDD 选择门禁

满足任一条件时加入 DDD 模块：

- 用户明确要求 DDD、四色建模、聚合、分包或领域模式；
- 存在丰富业务规则、状态迁移、不变量或长期演进的统一语言；
- 存在多业务能力、模型所有权冲突、跨模块一致性或外部系统防腐需求；
- 问题本质是上下文边界、聚合边界、依赖方向、职责放置或跨边界一致性错误。

简单 CRUD、一次性原型、纯技术适配或规则很薄时跳过 DDD 并简述理由。跳过 DDD 不等于跳过模块边界与依赖检查。命中时也可只做与问题相关的**局部**战术设计，不必每次铺满全套战略建模（见 [5-worked-example.md](references/5-worked-example.md) 示例二）。

## TDD 选择门禁

满足任一条件时加入 TDD 模块：

- 用户明确要求 TDD、测试先行或回归测试；
- 用户要求设计或改进测试命名、结构、分层、夹具、替身或可读性；
- 正在实现或修复业务规则、权限、状态、边界值、幂等、并发或副作用；
- 代码库已有可运行测试，变更需要回归保护；
- Bug 可先用稳定失败用例复现。

纯需求分析、纯开发计划、探索性原型或非可执行文档不进入红—绿—重构。若用户明确不要测试，遵从并报告未覆盖风险。

## 子智能体委派（可选执行优化）

这是 **Codex 执行层的可选优化，不是额外的方法阶段**。标准工作流是三拍:**① 主智能体先分析具体情况、切出子任务边界 → ② 并行委派子智能体执行 → ③ 主智能体验收、合并结果**。方法骨架、门禁逻辑与三个开发前必选章节一律不变。此处「智能体并行」指用多个子代理同时干活，与开发思路里「同层模块/代码切片可并行」是两码事，勿混淆。

前置：需在 `~/.codex/config.toml` 开启 `[features] multi_agent = true`，启用工具 `spawn_agent` / `wait_agent` / `close_agent`。

### 委派门禁（何时 fan-out）

满足**全部**条件才委派：

- 当前阶段能切出 ≥3 个边界清晰、无共享状态、可自包含描述的子任务；
- 并行收益明显大于扇入合并成本；
- 子任务产物能各自独立收敛，不需要彼此的中间结果。

简单任务、小修复、强耦合、需要全局共享上下文、或实现类会互相争用同一契约/迁移时**不委派**——允许结论就是「不委派、主智能体串行完成」（对齐「设计模式由变化点驱动，允许不使用模式」）。委派与否按复杂度缩放,简单 CRUD 不 spawn,如同简单任务跳过 DDD。

### 检测与降级（fan-out 前必查）

依次检查，任一不满足即降级为 solo（主智能体自己串行做）：

1. **递归守卫**：当前若已在子代理内执行，不再递归 spawn，直接 solo；
2. **能力可用**：`multi_agent` 未开启或 `spawn_agent` 工具不可用 → solo；
3. **运行时失败**：spawn 返回失败、`agent_type` 不可用或子代理无法启动 → 停止继续 spawn，改 solo，不把部分结果当完整结论。

无论委派还是降级,都在答复中一句话说明**实际执行方式与原因**（如「已并行委派 4 个需求角色梳理」或「multi_agent 未开启，降级 solo」）。skill 在未开启 `multi_agent` 的环境下默认走 solo，开箱即用。

### 各阶段拆分建议

| 阶段 | 可并行 fan-out | 必须由主智能体集中/串行 |
| --- | --- | --- |
| ① 需求分析 | 按角色/用例并行梳理 | 合并术语表、缝合跨角色交接链、消歧 |
| ② DDD | 每聚合/每上下文的局部战术设计 | 统一语言、Context Map、跨边界一致性矩阵 |
| ③ 开发思路 | **依赖图建立后**按同层模块/纵向切片 | 依赖图构建；避免并行切片争用同一契约或迁移 |
| ④ TDD | 独立切片/不同测试层并行 | 先建绿灯基线；外向内推进（暂停外层→窄测试→回外层）串行不可拆；证据汇总 |
| ⑤ 实现验证 | 独立切片并行编码（同③约束） | 扩大验证收敛 |

### 调用、验收与扇入

- 调用：`spawn_agent({ task_name, fork_turns: "none", message })` —— `fork_turns: "none"` 表示全新隔离上下文，故 `message` 必须**自包含**（子代理不继承会话历史，需带齐路径、范围、约束与期望产出格式）；`wait_agent({ timeout_ms })` 收集结果；`close_agent({ target })` 释放槽位。
- 约定：**分析/建模/审查类可并行；实现类默认串行**避免文件冲突。可复用随附模板 [agents/navigator-worker.toml](agents/navigator-worker.toml)（复制到 `~/.codex/agents/` 或项目 `.codex/agents/` 后按 `agent_type` 引用）。
- **验收（不可跳过）**：主智能体先**逐个验收**子代理产出——核对是否符合需求/验收标准、证据是否充分、有无臆造、越界或彼此冲突；不合格的**打回重派或自行补做**，绝不盲目采纳。子代理并行只加速产出，正确性由主智能体负责。
- **扇入合并（reconverge）**：验收通过后并回 `## 跨阶段追踪` 的单一链条与三个开发前必选章节；子产物视同「上游产物」，不留多份平行产物、不臆造规则；阻塞性业务决策仍由主智能体显式提给用户，不替用户拍板。

## 分发与执行规则

1. 读取用户请求、现有代码、文档与测试，区分事实、推断与未知项；先检查现有仓库约定，再提出结构或改代码。
2. 始终建立并在变更前输出需求分析、开发思路、验收标准；只对 DDD 与 TDD 应用门禁。上游产物可信时复用，不重复调查，但要摘要本次采用的结论。
3. 业务事实必须有用户输入、现有代码、测试或文档证据，不臆造规则；非阻塞未知项可采用明确、可逆的安全假设，阻塞性业务决策再询问用户。
4. 优先交付最小纵向闭环；权限、数据正确性与必要失败恢复不能以 MVP 为由删除。
5. 设计模式由变化点或复杂度证据驱动，允许结论是"不使用模式"；适配目标项目的语言、框架与目录习惯，不机械复制任何示例结构。
6. 保留并尊重现有测试，不能为绿灯弱化断言、吞异常或跳过测试。

## 跨阶段追踪

为必选阶段与实际选中的可选阶段维护最小追踪关系：

```text
需求/用例 → 模块或上下文（若有） → 开发切片 → 测试（若有） → 代码 → 验证证据
```

不得留下无对应需求的设计，也不得把范围内用例遗漏在计划之外。需求分析、开发思路和验收标准不得标注"未选"；未选 DDD 或 TDD 时在开发思路中标注"未选及理由"，不伪造产物。

## 交付格式

### 开发前消息

严格按以下顺序交付，三项缺一不可：

1. `## 需求分析`：核心目标、范围、关键用例、约束与未知项。
2. `## 开发思路`：现状/缺口、实现范围、纵向切片或步骤、依赖、验证方式与风险；命中 DDD/TDD 时在此说明采用的设计与测试先行策略，未命中时简述理由。
3. `## 验收标准`：逐条列出二元可判定的完成条件，优先使用 Given/When/Then，并包含必要的禁止性结果。

### 最终答复

开发完成后只汇报：

1. **实施结果**：实际修改文件与关键决策。
2. **验收结果**：逐条对照开发前验收标准给出通过/未通过及证据。
3. **偏差与未决风险**：只列真实偏差、未覆盖项和需用户决定的事项。

不要在最终答复中首次输出或事后重写需求分析、开发思路、验收标准。只有用户明确要求汇总完整过程时才可简要引用，但开发前消息仍是唯一实施基线。

## 资料基础

本 Skill 综合提炼需求分析、领域设计、开发规划与测试驱动开发资料。四色建模作为连接需求分析与领域设计的标准建模步骤。[5-worked-example.md](references/5-worked-example.md) 提供技术栈中立的端到端走查,用于校准输出形态。子智能体委派的预检降级与并行 fan-out 范式,借鉴 Codex 生态中 story-review 与 maestro-flow 的实践。
