---
name: navigate-software-development
description: 软件开发流程助手：在编写代码、修复缺陷或重构前，先向用户输出「需求分析」「开发思路」「验收标准」，方案明确后再开始实现与验证；任务完成后顺手将有价值的踩坑经验与设计决策沉淀到项目知识库。根据任务需要按需引入 DDD（领域设计）、TDD（测试驱动）与 Git Flow（版本控制）。触发场景包括：需求分析、用例梳理、开发方案、模块拆分；DDD、限界上下文、聚合建模、领域事件；TDD、测试先行、回归测试；分支策略、提交规范、PR 与代码评审；功能开发、Bug 修复、重构。严禁先改代码再补写方案。
---

# 开发罗盘

规范开发全流程的辅助指令。在动任何代码前，必须先理清并向用户输出**需求分析、开发思路、验收标准**；**DDD（领域设计）**、**TDD（测试驱动）**与 **Git Flow（版本控制）**按需启用，不生搬硬套。

## 执行流程

开发流程严格分为两个阶段：**先理清方案并告知用户，再动手实现与验证。**
允许先通过只读操作查阅代码，但严禁在方案发给用户前做任何写操作或环境修改。

```text
阶段一（准备·只读）：查阅代码/文档 → 需求分析 → 评估是否需要 DDD/TDD/Git Flow → 开发思路 → 验收标准
                                   ↓
                   先向用户输出三项说明（需求分析 + 开发思路 + 验收标准）
                                   ↓
阶段二（实施·可写）：现有测试基准 → 编写实现 → 测试验证 → 沉淀项目知识 → 对照验收标准汇报
```

1. **只读调研**：查阅用户请求、项目现有规范、代码、测试用例和文档。此阶段只允许搜索、读取文件、检查 Git 状态等只读操作，不修改任何文件。
2. **分析需求**：参考 [1-requirements-analysis.md](references/1-requirements-analysis.md)，明确核心目标、场景用例与待确认事项。
3. **按需选用模块**：
   - 业务模型复杂时，参考 [2-ddd-design.md](references/2-ddd-design.md) 引入轻量领域设计；
   - 涉及核心业务规则、修复 Bug 或防回归时，参考 [4-tdd.md](references/4-tdd.md) 引入测试驱动；
   - 用户明确要求分支/提交策略时，参考 [7-git-flow.md](references/7-git-flow.md) 引入版本控制规范；
   - 若某模块未选用，准备好一句话说明跳过理由，不盲目堆砌概念。
4. **规划开发思路**：参考 [3-development-planning.md](references/3-development-planning.md)，整合上述设计、测试策略与实现步骤，形成清晰的开发思路。
5. **制定验收标准**：列出清晰可核验（非是即否、成功/失败界限明确）的验收项，与开发步骤一一对应。
6. **先发方案再动手**：**在执行任何修改代码的操作之前**，必须先在回复开头按顺序输出以下三个二级标题：
   - `## 需求分析`
   - `## 开发思路`
   - `## 验收标准`
7. **进入实施**：若用户要求编码实现，方案输出后即可立即动手开发，无需等待用户确认；但若存在阻塞性未知项、高风险/不可逆操作，或用户明确要求先审批，则暂停并等待用户答复。
8. **纯方案任务及时停止**：若用户明确只要分析或方案，输出上述三项后立即停止，不要擅自改代码。

输出内容可根据任务复杂度做长短缩放；不确定输出形态时，可参考 [5-worked-example.md](references/5-worked-example.md) 中的示例。

### 输出格式红线

上述三个二级标题必须位于**当前回复的最开头（首个文本块）**，之后才允许调用任何修改文件或执行变更的工具。以下情况均属违规：

- 本轮回复已经执行了修改文件的操作，后面才输出这三个标题；
- 将标题改为加粗、合并成一个段落，或只写在内部思考中而不输出给用户；
- 改完代码后，才在最终总结里补写这三项——**事后补写视为违规**。

### 只读与改动的严格界限

- **方案发给用户前允许的操作（纯只读）**：查看目录、读取文件、搜索文本、查看 Git 状态/历史/差异、阅读既有测试文件和 CI 结果。
- **方案发给用户前严禁的操作（写操作）**：新建或编辑文件、自动代码格式化、安装或更新依赖、创建 Git 分支/提交/推送、执行数据库迁移，以及任何会修改环境的外部命令。
- **前期暂不实跑测试**：运行测试通常会产生缓存、修改测试数据库或生成覆盖率产物。因此在方案输出前只查阅测试代码和已有 CI 结果，不运行测试命令；在方案输出后、编写代码前，再运行现有测试建立测试基准。
- **拿不准的命令一律视为改动**：如果不确定某命令是否会写入文件或产生缓存，统一当作写操作处理，必须先发方案。
- **中途方案调整**：如果在开发过程中发现需求有重大变化或原方案走不通，应立即暂停修改，重新向用户输出修订后的三个章节，说明变更原因后再继续，严禁在最终汇报中悄悄改写预期目标。

### 极小改动简化规则

当改动**同时**满足以下所有条件时，三项内容可以各简化为 1~2 句话，但仍必须在改动前输出：

- 只涉及单个文件；
- 不改变任何可观察的运行行为（如改错字、改注释、修文案、纯变量重命名、无逻辑分支的文案配置）；
- 改动效果一眼就能确认正确性，无需专门运行测试来验证；
- 绝不涉及权限、数据一致性、并发处理、数据库迁移、外部接口契约或升级依赖版本。

只要有任意一条不满足，就必须走完整方案。简化规则仅缩减文字篇幅，**绝不能省略三个标题**，也必须在动手改代码前输出。拿不准时一律按完整方案执行。

## 任务类型与处理规则

| 用户需求类型 | 开发前必须输出 | 处理动作与停止时机 |
| --- | --- | --- |
| 梳理需求、分析范围、整理用例与验收条件 | 必须输出三项；开发思路可简写 | 输出方案后立即停止，不擅自写代码 |
| 梳理开发方案、模块拆分、技术评估 | 必须输出三项 | 输出方案后立即停止，不擅自写代码 |
| 复杂业务领域建模（DDD、模型边界、聚合等） | 必须输出三项；开发思路中融入领域设计 | 输出方案与模型设计后停止，不擅自写代码 |
| 测试设计、测试重构、TDD 测试先行 | 必须输出三项；开发思路中明确测试策略 | 输出方案与测试设计；仅在用户要求时写代码 |
| 分支管理、规范提交、PR 与代码评审 | 必须输出三项；开发思路中说明 Git 策略 | 输出方案与 Git 策略；仅在用户要求时执行操作 |
| 完整功能开发或重构 | 先输出三项方案，再开始开发 | 完成编码与测试验证后汇报结果 |
| 修复 Bug | 先输出三项方案，再开始修复 | 编写复现测试并修复验证后汇报结果 |
| 纯文案/格式微改（满足极小改动全部条件） | 三项各简写为 1~2 句，动代码前输出 | 完成修改并快速核验后汇报结果 |

## 领域设计（DDD）触发条件

满足以下任一条件时，引入领域设计模块：

- 用户明确要求使用 DDD、领域建模、聚合划分或领域设计模式；
- 业务规则复杂、状态流转多变，涉及核心业务模型与约束；
- 涉及多个业务能力协作、模型边界冲突、跨模块数据一致性，或需要对外部系统做防腐隔离；
- 核心问题在于模块边界不清、职责划分混乱或依赖方向错误。

如果是简单的增删改查（CRUD）、临时一次性脚本或简单技术适配，则**跳过 DDD 并用一句话说明理由**。跳过 DDD 不代表可以乱写代码，仍要保持模块清晰。触发时参考 [2-ddd-design.md](references/2-ddd-design.md) 的分级指引，只做当前问题最需要的轻量设计，切忌过度设计（参见 [5-worked-example.md](references/5-worked-example.md) 示例二）。

## 测试驱动（TDD）触发条件

满足以下任一条件时，引入测试驱动模块：

- 用户明确要求使用 TDD、测试先行或补充回归测试；
- 用户要求设计或优化测试用例结构、命名、分层、Mock 或 Fixture 策略；
- 正在实现或修复重要业务规则、权限控制、状态流转、边界值、幂等性、并发或副作用逻辑；
- 项目中已有可运行的测试套件，本次改动需要防止功能回归；
- 修复 Bug 时，能够先编写一个稳定的失败测试用例来精准复现问题。

若任务仅是需求分析、方案探讨、原型摸索或文档撰写，不执行测试流程，跳过时附带一句话说明理由。若用户明确说明不需要测试，尊重用户意愿并在最终汇报中提示未测试风险。

## 版本控制（Git Flow）触发条件

**只有在用户明确提出版本控制相关要求时，才引入 Git Flow 模块。** 满足以下任一情况即视为用户明确要求：

- 用户明确提出 Git Flow、特定分支规范（如 GitHub Flow、主干开发等）或分支隔离；
- 用户要求规范提交信息（如 Conventional Commits）或控制 commit 粒度；
- 用户要求创建 PR、代码评审（Code Review）、合并方式或遵循主干保护规则；
- 用户要求将代码合并入受保护的分支并遵循其规则。

**注意：仓库本身的客观状态不会自动触发该流程。** 即使项目是多人团队协作、存在 `main`/`master` 受保护主干，只要用户没有明确提出 Git 流程要求，一律跳过 Git Flow，并在开发思路中用一句话说明理由（例如“用户未指定分支与提交策略，按默认方式处理”）。用户未提出时，不要强加复杂的 Git 分支与提交流程。

当用户明确要求时，参考 [7-git-flow.md](references/7-git-flow.md) 采用最契合要求的轻量方案。Git Flow 与 DDD、TDD 相互独立，互不影响。

## 项目经验与知识沉淀

在每次改动代码并通过验证后，顺手评估**本次开发是否有离开会话就会遗失、未来还会用到的重要信息**。如果有，整理沉淀到项目的知识库中（默认记录在 `KNOWLEDGE.md`，若项目已有约定则顺应既有规范）：

- **值得沉淀的内容**：踩坑排错经验、特殊限制条件、关键架构设计决策、第三方外部接口特殊约定等；
- **无需沉淀的内容**：看代码签名一目了然的信息（如“新增了一个参数”）、代码变更记录（已有 Git/CHANGELOG 记录）、通用的编程基础知识；
- **持续维护而不是堆流水账**：如果发现已有条目过时或属于同一主题，应主动更新、合并或修正已有内容，而不是无脑往后追加流水账；
- **若无内容可沉淀**：纯需求分析、只读查看、无逻辑变更的极小修改，或本次没有暗坑与特殊决策时，跳过此步骤并在最终汇报中用一句话说明跳过理由。详细准则参考 [8-knowledge-base.md](references/8-knowledge-base.md)。

## 多任务并行与子代理派发（可选）

如果遇到非常庞大且各子模块相互独立的复杂任务，可以将无状态冲突的子任务派发给子代理（Subagent）并行处理，最后统一汇总合并。

- **默认单人模式**：绝大多数任务直接单上下文完成，不需要派发子代理；
- **规则不变**：无论是否派发子代理，方案前置（需求分析、开发思路、验收标准）的规则完全不变；
- 平台能力与并行分发细节参考 [6-delegation.md](references/6-delegation.md)（仅在需要并行处理时查阅）。

## 编码与实施规范

1. **尊重现有代码与规范**：先检查项目已有的代码风格、设计模式和命名规范，顺应现有风格，不自作主张重构不相干代码。
2. **基于事实，不瞎猜规则**：业务逻辑必须有用户说明、代码逻辑、测试用例或文档作为依据，不主观臆造。对于非关键细节可采用安全、可回退的合理推断；若涉及核心业务决策或关键分歧，主动询问用户。
3. **小步快跑，小闭环交付**：优先实现能完整跑通、可验证的最小功能。但安全权限、数据准确性和必要的异常处理绝不能以“这只是初版”为借口偷工减料。
4. **拒绝设计模式泛滥（Google 实践）**：坚持简单优先、组合优于继承，不为未来臆想的扩展过早抽象；能用简单函数解决的不搞复杂继承与工厂。
5. **代码注释规范（Google 风格）**：注释重在解释“为什么这么做”（Why）而非复述“在做什么”（What）；公共接口提供标准文档注释；禁止留存废弃的大段被注释代码。
6. **坚守测试真实性**：绝对不能为了让测试通过而故意弱化断言、吃掉异常或注释掉既有测试。
7. **收尾评估知识沉淀**：改动验证通过后，按规则评估并更新项目知识库。

## 方案自查清单（输出前核对）

在向用户发出方案之前，自我核对以下关键项：

1. **范围分明**：明确写出“本次要做”、“后续再做”或“本次不做”的界限；
2. **依据充分**：分清已有事实与推测，关键业务决策未擅自拍板；
3. **步骤清晰**：列出清晰的实现步骤，每个步骤包含改动范围与验证方式；
4. **验收明确**：验收标准具体可测（成功/失败界限清晰），禁止使用“功能正常”、“性能良好”等模糊词汇，必要时包含负向/异常防范条件；
5. **跳过有据**：若未采用 DDD / TDD / Git Flow，均已用一句话说明跳过理由；
6. **位置合规**：三个二级标题位于回复的最开头，其后没有任何未经说明的写操作。

各模块详细指引：[需求分析](references/1-requirements-analysis.md#门禁) · [领域设计](references/2-ddd-design.md#121-交付门禁) · [开发规划](references/3-development-planning.md#门禁) · [测试驱动](references/4-tdd.md#8-验证阶段与自查清单) · [版本控制](references/7-git-flow.md#5-门禁产物与反例)

## 方案与实现的一致性追踪

确保开发过程从头到尾前后呼应：

```text
需求与用例 → 涉及模块 → 开发步骤 → 测试用例（若有） → 代码实现 → 验收验证 → 知识沉淀（若有）
```

- 方案里写了的需求，步骤中必须有对应实现，绝不遗漏；
- 方案里没要求的逻辑，不在代码中擅自夹带私货；
- 需求分析、开发思路和验收标准三项必写，不可跳过；可选模块若未选用，在开发思路中简述理由即可。

## 回复格式规范

### 1. 动代码前的方案说明（首个文本块）

严格按顺序输出以下三项：

1. `## 需求分析`：核心目标、影响范围、关键场景用例、约束与待确认事项。
2. `## 开发思路`：现状分析、实现步骤、依赖关系与潜在风险；若选用 DDD/TDD/Git Flow，在此说明具体方案；若未选用，写明跳过理由。
3. `## 验收标准`：逐条列出清晰、可直接核验的完成条件（建议采用 Given/When/Then 或明确的“输入-预期结果”格式），并包含关键的异常防范条件。

### 2. 开发完成后的结果汇报

代码改完并通过测试后，汇报以下内容：

1. **实施结果**：具体修改了哪些文件、做了哪些关键设计决策；
2. **验收结果**：逐条对照开发前给出的验收标准，说明测试与验证情况（附带通过证据）；
3. **本次沉淀**：更新或新增了哪些项目知识库条目及文件路径；若本次没有需要沉淀的内容，写明跳过理由（详见 [8-knowledge-base.md](references/8-knowledge-base.md)）；
4. **遗留问题与风险**：是否存在尚未覆盖的边界场景或需要用户后续确认的事项。

严禁在改完代码后才补发方案，也不要在最终汇报里悄悄篡改最初的验收目标。

## 资料基础

本 Skill 综合提炼了敏捷开发、需求分析、领域建模、开发规划与测试驱动开发实践。对于不同任务形态的完整走查示例，可参考 [5-worked-example.md](references/5-worked-example.md)。
